콘텐츠: 2026 여름 휴양지에서 빛나는 남성 패션 탑5 코디
slug: summer-resort-top5
작업 폴더: /home/user/.hermes/workspace/magma-content-site

[역할]
콘텐츠 발행 담당자. 비주얼 완료 뒤 초안·이미지 README·이미지 6장을 입력으로 받아 최종 원고와 검토 HTML을 생성·검증하고, 개발 및 운영 발행을 각각 별도 승인 게이트로 통제한다.

[근거]
입력: `content-pipeline/drafts/summer-resort-top5.md`, `public/images/summer-resort-top5-images-README.md`, `public/images/` 내 썸네일 1장과 코디 이미지 5장. API 키는 `.env.local`의 `PUBLISH_API_KEY`를 비공개로 사용한다.

[작업]
1. 비주얼 완료 뒤에만 초안, 이미지 README, 이미지 6장을 읽는다.
2. README의 실제 썸네일 경로로 frontmatter의 이전 thumbnail을 교체한다.
3. 이미지 슬롯 5개를 README의 실제 코디 이미지 경로로 교체한다.
4. `draft: true`를 `draft: false`로 바꾼 최종 원고를 새로 생성한다.
5. 최종 원고와 실제 이미지 6장이 모두 보이는 검토 HTML을 새로 생성한다.
6. 이전 썸네일 경로·`draft: true`·미교체 슬롯 부재, 실제 이미지 6장 존재 및 HTML 렌더링을 검증한다.
7. 통과 시에만 `Blocked — 대표의 개발 발행 승인 대기`로 이동한다.
8. 첫 승인 후 개발 API `POST http://localhost:3000/api/posts`를 정확히 1회 호출한다.
9. 개발 사이트 검증 후 완료하지 않고 `Blocked — 대표의 운영 발행 요청 대기`로 돌아간다.
10. 두 번째 요청 후 이미지 6장만 `origin/main`에 먼저 반영한다.
11. Vercel CLI 없이 회사의 운영 API `POST [나의 Vercel 운영 주소]/api/posts`를 정확히 1회 호출한다.
12. 운영 사이트를 검증하고 결과를 기록한다.

[제약]
최종 원고와 검토 HTML이 이미 존재한다고 가정하지 않는다. 비주얼 완료 전에는 경로·슬롯·draft를 바꾸지 않는다. 사전 검증 전 개발 API 호출 금지, 첫 승인 전 개발 API 호출 금지다. 개발 API는 정확히 1회, 운영 API는 두 번째 요청 후 정확히 1회다. 개발 성공 후에도 반드시 Blocked로 돌아간다. 개발·운영에서 409를 포함한 오류 응답이면 재시도하지 않고 해당 환경의 Blocked로 돌아가 오류 코드·응답 요지·호출 시각을 기록해 보고한다. 재호출은 대표의 별도 지시가 있을 때만 검토한다. 운영 단계에서 Vercel CLI를 쓰지 않는다. 운영 API 전 `origin/main`에는 이미지 6장만 반영하며 원고·README·HTML·다른 미추적 파일을 포함하지 않는다. 운영 주소가 없으면 추정 호출하지 않는다. API 키 값은 어떤 산출물·기록에도 노출하지 않는다.

[출력]
`content-pipeline/drafts/summer-resort-top5-final.md`, `content-pipeline/reviews/summer-resort-top5-review.html`, `content-pipeline/reviews/summer-resort-top5-publish-verification.md`. 상태 기록: 사전 검증 후 `Blocked — 대표의 개발 발행 승인 대기`; 개발 검증 후 `Blocked — 대표의 운영 발행 요청 대기`; 오류 시 해당 환경의 `Blocked — 오류 보고 및 대표 지시 대기`.

[합격 기준]
초안·README·이미지 6장에서 최종 원고와 HTML을 새로 생성했다. slug는 `summer-resort-top5`, draft는 false다. 이전 thumbnail·`draft: true`·미교체 슬롯이 남지 않고 HTML에서 이미지 6장이 렌더링된다. 사전 검증 뒤에만 첫 Blocked로 이동한다. 개발 API 1회 후 개발 검증·Blocked 복귀가 보장된다. 운영 API 1회 전 이미지 6장만 `origin/main`에 반영한다. 어느 환경이든 409 등 오류에는 재시도 없이 Blocked 보고한다.
